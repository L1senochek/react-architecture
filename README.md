# Домашнее задание: Архитектура React-приложений

**(максимум — 12 баллов)**

## Цель

Закрепить принципы архитектурного проектирования React-приложений на основе
Feature-Sliced Design (FSD), научиться разделять ответственность между слоями и
применять FSD в практическом мини-проекте.

## Основная часть (до 10 баллов)

### 1. Создание проекта по FSD (3 балла)

#### Задание

Создайте новый проект на React с TypeScript и настройте базовую архитектуру по
принципам Feature-Sliced Design (FSD).

#### Что нужно сделать

a. Инициализируйте проект через Vite (npm create vite@latest).
b. Настройте такую структуру каталогов внутри src/:

```
src/
  app/
  entities/
  features/
  shared/
  pages/
  widgets/
```

Что должно находиться в app/:

- App.tsx — базовый корневой компонент.
- router.tsx — файл с определением маршрутов.

c. Настройте ESLint и Prettier с конфигурацией, отражающей принципы FSD:
поддержка абсолютных импортов через tsconfig paths. Рекомендуется
подключить алиасы сразу, чтобы избежать хаоса с относительными путями на
следующих этапах. Пример tsconfig.json с путями:

```
{
 "compilerOptions": {
   "baseUrl": "src",
   "paths": {
     "shared/*": ["shared/*"],
     "entities/*": ["entities/*"],
     "features/*": ["features/*"],
     "widgets/*": ["widgets/*"],
     "pages/*": ["pages/*"],
     "app/*": ["app/*"]
   }
 }
}
```

#### Критерии:

- Проект запускается без ошибок — 1 балл.
- Структура соответствует FSD — 1 балл.
- Настроены ESLint и Prettier с конфигурацией, отражающей принципы
  FSD, — 1 балл.

### 2. Реализация сущности Task (3 балла)

#### Задание

Создайте бизнес-сущность task в слое entities/. Опишите тип, реализуйте
UI-компонент для отображения задачи и примените стилизацию через CSS-модули.

#### Интерфейс задачи:

```
// entities/task/model/types.ts
export interface Task {
 id: string;
 title: string;
 completed: boolean;
}
```

#### Компонент отображения:

Создайте компонент TaskCard.tsx — он должен быть презентационным, то есть:

- принимает данные через props;
- не содержит логики или side-effects;
- рендерит заголовок и статус (например, галочка/иконка/цвет);
- использует CSS-модули для стилизации.

#### Рекомендуемая структура папок:

```
entities/
task/
model/
types.ts
ui/
TaskCard.tsx
TaskCard.module.css
Стили для TaskCard должны располагаться рядом с компонентом, в файле
TaskCard.module.css.
```

#### Критерии:

- Типизация и структура — 1 балл.
- Компонент TaskCard с правильной презентационной логикой — 1 балл.
- Стили через .module.css — 1 балл.

### 3. Создание виджета «Список задач» (3 балла)

#### Задание

Реализуйте фичу taskList в слое widgets/. Она должна отображать список задач и
обеспечивать интерактивные действия: фильтрацию по статусу и удаление.

#### Рекомендуемая структура папок

```
widgets/
taskList/
model/
      useTasks.ts
    ui/
      TaskList.tsx
```

Используйте кастомный хук useTasks. API хука:

```
// widgets/taskList/model/useTasks.ts
import { Task } from 'entities/task/model/types';

export type Filter = 'all' | 'completed' | 'incomplete';

function useTasks(initial: Task[]): {
 tasks: Task[];                   // отфильтрованные задачи
 filter: Filter;                  // текущий фильтр
 setFilter: (f: Filter) => void;  // смена фильтра
 removeTask: (id: string) => void; // удаление задачи по ID
}
```

В качестве «начальных данных» для этого задания можно использовать локальный
массив прямо внутри хука (например, useState([...])).

#### Требования к логике:

- Задачи фильтруются по статусу:
  - "all" — все задачи;
  - "completed" — только завершенные;
  - "incomplete" — только незавершенные.

- При удалении задачи она исчезает из UI без перезагрузки.
- Все действия должны отражаться сразу в интерфейсе.

#### Критерии:

- Хук корректно реализует фильтрацию и удаление — 1 балл.
- Компонент TaskList правильно отображает отфильтрованные задачи — 1
  балл.
- Используется state-хук и проброс пропсов — 1 балл.

### 4. Вывод задач на странице (2 балла)

#### Задание

a. Создайте страницу TaskPage в слое pages/, которая будет отображать список
задач с фильтрацией.

#### Структура:

```
pages/
  tasks/
    ui/
      TaskPage.tsx
```

b. Внутри страницы подключите виджет TaskWidget, реализующий визуальную
обертку над TaskList, фильтрацией и логикой управления задачами.

#### Пример импорта:

```
// pages/tasks/ui/TaskPage.tsx
import { TaskWidget } from 'widgets/task';

export function TaskPage() {
 return (
   <div>
     <h1>Мои задачи</h1>
     <TaskWidget />
   </div>
 );
}
```

#### Критерии:

- Страница и виджет работают корректно — 1 балл.
- Читаемый, чистый и модульный код — 1 балл.

### Дополнительная задача (1 бонусный балл)

Эта задача не обязательна, но может помочь набрать баллы до 12.
Вынесите кнопку фильтрации в shared-компонент (1 балл)

- Создайте компонент FilterButton в shared/ui.
- Используйте его внутри виджета TaskList.

---

# Домашнее задание 2: Оптимизация производительности в React

**(максимум — 12 баллов)**

## Цель

Закрепить на практике методы оптимизации React-приложений: минимизация лишних ререндеров, работа с мемоизацией и использование React DevTools.

## Основная часть (до 9 баллов)

### 1. Оптимизация TaskCard с помощью React.memo (3 балла)

#### Задание

Переведите `TaskCard` (из `entities/task/ui/`) в memo-компонент.

#### Требования

- Используйте `React.memo`.
- Проверьте, что компонент не перерисовывается без изменения `props`.

#### Критерии

- Компонент обернут в `React.memo` — 1 балл.
- Поведение корректное, `props` не создаются заново без необходимости — 1 балл.
- Код читаемый и чистый — 1 балл.

### 2. Мемоизация списка задач с useMemo (3 балла)

#### Задание

В хук `useTasks` добавьте `useMemo` для мемоизации отфильтрованного массива задач. В качестве «начальных данных» для этого задания используется локальный массив, передаваемый в хук `useTasks`.

#### Требования

- Фильтрация по статусу (`all`, `completed`, `incomplete`) выполняется через `useMemo`.
- Изменение фильтра или списка задач должно корректно отражаться в UI.

#### Граничные случаи

Обязательно протестируйте:

- Поведение при удалении задачи или изменении её статуса — `useMemo` не должен «запоминать» устаревшие данные.
- Сценарии, когда список задач пустой или содержит одну задачу.

#### Критерии

- `useMemo` применяется для фильтрации — 1 балл.
- UI обновляется корректно — 1 балл.
- Поведение не ломается — 1 балл.

### 3. Мемоизация функций с useCallback (3 балла)

#### Задание

В хуке `useTasks` оберните функцию `removeTask` в `useCallback`, чтобы ссылка на неё оставалась стабильной между рендерами. Мемоизация `removeTask` позволяет избежать лишних перерисовок `TaskCard`, если они обернуты в `React.memo`.
Без useCallback функция будет создаваться заново на каждый рендер, и React.memo не сможет «поймать» ее, как в прошлой версии.

#### Требования

- Мемоизированные функции передаются в `props` компонентов.
- Функции не создаются заново при каждом рендере.

#### Критерии

- Используется `useCallback` — 1 балл.
- Функции корректно передаются — 1 балл.
- Поведение не ломается — 1 балл.

## Дополнительная задача (3 бонусных балла)

### Анализ производительности через DevTools (3 балла)

#### Задание

Проведите профилирование проекта с помощью React DevTools.

Что нужно сделать:

- Запустить Profiler, выполнить интеракции с приложением (фильтрация, удаление задач).
- Сделать скриншот профиля с краткими заметками (что улучшили, что осталось).
- Описать минимум два наблюдения (например, какие компоненты часто перерисовываются).

#### Критерии

- Есть скриншот работы с Profiler — 1 балл.
- Есть комментарии к скриншоту с пояснениями — 1 балл.
- Есть анализ 2+ компонентов — 1 балл.

#### Куда загружать скриншот

- Если вы работаете с GitHub или GitVerse: добавьте скриншот и текст наблюдений в файл `README.md` вашего проекта.

---

#### Скриншоты Profiler

- https://ibb.co/JFn5TQG6
- https://ibb.co/ymV4vf2B
- https://ibb.co/TMNgcCL7
- https://ibb.co/YBR2bGRr

**Комментарии к скриншотам:**

При смене фильтра/нажатии на кнопку удалить перерендериваются компоненты TaskWidget, TaskList. В браузере видно какие компоненты ререндерятся, они обведены голубой рамкой где подписано название компонентов.
В плагине видна скорость загрузки страницы при первом рендере.
Так же в плагине в консоле можно увидеть какие компоненты мемоизированны(в скобочках рядом с названием компонента указаны), какое количество времени затрачено на ререндер каждого из них.

1. TaskCard обернутый в memo не перерендеривается при удалении другой задачи
2. TaskList перерендеривается при каждом изменении списка (изменение фильтров/удаление)
3. TaskWidget & FilterButton & TaskFilters всегда перерендериваются

---

# Домашнее задание 3: RTK Query и управление задачами

**(максимум — 12 баллов)**

## Цель

Научиться интегрировать RTK Query для загрузки данных с API и реализовать удаление
задач на клиенте, сохраняя загруженные данные в локальном состоянии.

## Основная часть (до 9 баллов)

### 1. Создание API-модуля через RTK Query (3 балла)

#### Задание

Создайте tasksApi в entities/task/api, используя createApi и fetchBaseQuery.
Настройте загрузку задач с сервера https://jsonplaceholder.typicode.com/todos.

#### Структура папок

```
entities/
└── task/
├── api/
│   └── tasksApi.ts
├── model/
└── ui/
```

#### Пример того, что приходит от сервера

```
[
  {
    "userId": 1,
    "id": 1,
    "title": "delectus aut autem",
    "completed": false
  }
]
```

Что нужно вернуть из transformResponse: `(response) => response`

#### Пример шаблона tasksApi.ts

```
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';
import { Task } from '../model/types';

export const tasksApi = createApi({
  reducerPath: 'tasksApi',
  baseQuery: fetchBaseQuery({ baseUrl: 'jsonplaceholder.typicode.com' }),
  tagTypes: ['Tasks'],
  endpoints: (build) => ({
    getTasks: build.query<Task[], void>({
      query: () => 'todos',
      transformResponse: (response: Task[]) => response,
    }),
  }),
});

export const { useGetTasksQuery } = tasksApi;
```

#### Требуется

- Настроить reducerPath, baseQuery, tagTypes.
- Реализовать endpoint getTasks, использующий transformResponse, чтобы выделить массив todos.

#### Критерии

- API корректно описан и экспортирует хук useGetTasksQuery — 1 балл
- Загрузка задач происходит без ошибок — 1 балл
- Используется transformResponse для получения массива — 1 балл

### 2. Отображение задач в интерфейсе (3 балла)

#### Задание

Отобразите полученные задачи из useGetTasksQuery в компоненте TaskList,
используя презентационный компонент TaskCard.
Для этого надо реализовать хук useTasks, в котором:

- данные загружаются через useGetTasksQuery;
- данные копируются в локальное состояние (useState);

#### Требуется

- Загруженные задачи передаются в TaskList.
- Внутри TaskList создается список из TaskCard.
- Правильная типизация и структура кода.

#### Критерии

- Реализован хук useTasks,в котором данные загружаются через useGetTasksQuery — 1 балл;
- Задачи отображаются в интерфейсе — 1 балл
- Код структурирован и соответствует FSD — 1 балл

### 3. Локальное удаление задачи (3 балла)

#### Задание

Реализуйте в хуке useTasks функцию removeTask(id: number), удаляющую задачу из useState.
Важно: не используйте запросы на сервер — только локальное удаление.

#### Рекомендации

- Используйте useEffect, чтобы скопировать загруженные задачи один раз.
- Избегайте бесконечного цикла обновлений!
  Можно использовать зависимость remoteTasks.length === 0 или пустой массив зависимостей, если копирование выполняется один раз.
  Альтернатива: проверка, что локальный массив еще пуст.

#### Критерии

- Использован useEffect для загрузки данных в useState — 1 балл
- Реализована функция removeTask — 1 балл
- После удаления задача исчезает из UI — 1 балл

## Дополнительная задача (3 бонусных балла)

### Общий baseApi + injectEndpoints для tasksApi (3 балла)

#### Задание

Вынесите общий экземпляр RTK Query в модуль baseApi и используйте его для
добавления эндпоинтов задач через injectEndpoints.
Это позволит иметь единый reducerPath и middleware для всего приложения.

#### Требуется

- Создать общий baseApi в shared/api с reducerPath: 'api', базовой baseUrl и tagTypes, включая 'Tasks'.
- Подключить один раз baseApi.reducer и baseApi.middleware в store.
- Переписать tasksApi на baseApi.injectEndpoints(...), добавить эндпоинт getTasks с transformResponse → response.todos.
- Существующие места использования useGetTasksQuery должны работать без изменений.

#### Критерии

- baseApi создан и экспортирован из shared/api/baseApi.ts (или shares/...), tagTypes включает Tasks — 1 балл
- tasksApi использует injectEndpoints от baseApi; getTasks корректно возвращает Task[] — 1 балл
- в store подключены baseApi.reducer и baseApi.middleware один разз — 1 балл

---

# Домашнее задание 4: Управление формами

**(максимум — 10 баллов)**

## Цель

Закрепить навыки работы с формами в React: от базового управления состоянием до динамических полей и схем валидации. Научиться выбирать подходящий инструмент (Formik, React Hook Form или useActionState) под конкретную задачу и реализовывать формы, устойчивые к ошибкам.

## Основная часть (до 10 баллов)

### 1. Создание формы регистрации пользователя (4 балла)

#### Задача

Создайте страницу с формой регистрации пользователя, включающей поля:

- Имя пользователя (username).
- Email.
- Пароль.
- Подтверждение пароля.

#### Что нужно сделать?

- Реализовать форму с использованием React Hook Form или Formik.
- Добавить базовую валидацию (можно временно вручную или сразу через схему). Если сразу используете схему — пункты шага 2 считаются выполненными:
  - Все поля обязательны.
  - Email должен содержать @.
  - Пароль — минимум 6 символов.
  - Подтверждение пароля должно совпадать с паролем.

#### Критерии

- Использован один из рекомендованных подходов (Formik или RHF) — 1 балл.
- Все поля формы обязательны для заполнения — 1 балл.
- Реализована корректная валидация всех полей — 1 балл.
- Ошибки отображаются рядом с соответствующими полями — 1 балл.

### 2. Подключение yup или zod (2 балла)

#### Задание

Перенесите валидацию из «ручной» формы в схему. Используйте yup или zod по выбору. Если вы сразу использовали схему в Шаге 1, дайте короткий комментарий или ссылку — этот шаг считается выполненным с yup (если используется Formik) или zod (если используется RHF).

#### Уточнения

#### Formik

- Вариант A: yup.object({...}) через validationSchema.
- Вариант B: zod — через validate (внутри вызывать schema.safeParse) или адаптер.

#### React Hook Form

- Используйте любой резолвер: zodResolver(schema) или yupResolver(schema) из @hookform/resolvers.

#### Критерии

- Использована схема валидации вместо ручной логики — 1 балл.
- Сообщения об ошибках отображаются корректно рядом с соответствующими полями — 1 балл.

### 3. Динамическое поле: социальные ссылки (4 балла)

#### Задача

Добавьте к форме блок социальных ссылок — список полей, где пользователь может указать свои профили (например, GitHub, GitVerse или соцсети). По умолчанию отображается одно поле, пользователь может добавить или удалить любое количество ссылок.

#### Что нужно сделать?

- Использовать useFieldArray (в случае RHF) или FieldArray (Formik).
- Добавить кнопки «Добавить ссылку» и «Удалить».

#### Критерии

- Использованы useFieldArray (в случае RHF) или FieldArray (Formik) — 1 балл.
- Форма корректно обрабатывает массив ссылок — 1 балл.
- Добавление и удаление работает без ошибок — 1 балл.
- Каждое поле валидируется как URL — 1 балл:
  - Для Yup: .url('Некорректный URL')
  - Для Zod: z.string().url('Некорректный URL').

### Дополнительное задание (2 бонусных балла)

#### Задача

Реализуйте мини-форму в стиле wizard (2 шага), используя новый встроенный API из React 19 — useActionState.

#### Пример сценария:

1. Шаг 1 — Ввод email.
2. Шаг 2 — Подтверждение подписки.

#### Отображайте UI-состояния:

- pending — при отправке;
- error — если email не указан или невалидный;
- success — сообщение после успешной отправки.

#### Критерии

- Реализована форма с использованием useActionState — 1 балл.
- Сообщения об ошибках или успешной отправке отображаются в UI — 1 балл.

#### Результат

В результате выполнения:

- Научитесь реализовывать сложные формы с динамическими блоками.
- Освоите работу с yup и zod для декларативной валидации.
- Получите опыт выбора подходящего инструмента под задачу и реализовывать формы, устойчивые к ошибкам.
